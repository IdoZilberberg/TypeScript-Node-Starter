import request from "supertest";
import app from "../src/app";

describe("Send to scanner", () => {
  // it("should return 404", (done) => {
  //   request(app).get("/reset")
  //     .expect(404, done);
  // });

  // it("should GET local file path and scan it", (done) => {
  //   request(app)
  //     .get("/scanner/file")
  //     // .query({"localpath": "files/nothing.txt"})
  //     .expect(200, done);
  // });
});
